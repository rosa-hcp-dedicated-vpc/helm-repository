# Web Terminal Operator Configuration
appTeam: web-terminal-operator

# Sync wave for ArgoCD deployment
syncwave: 1

# Operator configuration
operatorName: web-terminal
operatorNamespace: openshift-operators
operatorChannel: fast
operatorSource: redhat-operators
operatorSourceNamespace: openshift-marketplace
operatorInstallPlanApproval: Automatic

# Helper operator dependency
helper-operator:
  enabled: true
  syncwave: 1
  operatorName: web-terminal
  operatorNamespace: openshift-operators
  operatorChannel: fast
  operatorSource: redhat-operators
  operatorSourceNamespace: openshift-marketplace
  operatorInstallPlanApproval: Automatic

# Helper status checker dependency
helper-status-checker:
  enabled: true
  syncwave: 2
  operatorName: web-terminal
  operatorNamespace: openshift-operators
  maxWaitTime: 600

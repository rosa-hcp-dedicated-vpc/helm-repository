{{- if .Values.lightspeed.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: lightspeed-mcp-token
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  mcpToken: {{ .Values.lightspeed.mcpToken | quote }}
{{- end }}


